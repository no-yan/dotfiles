diff --git a/.zshrc b/.zshrc
index eb147396a16bfe99eed8b49f2a2a37b037e25331..ccf8de0fbdea64779f3bd73baecc473bc0ef0571 100644
--- a/.zshrc
+++ b/.zshrc
@@ -21,22 +21,6 @@ # llvm
 # export LDFLAGS="-L/opt/homebrew/opt/llvm/lib"
 # export CPPFLAGS="-I/opt/homebrew/opt/llvm/include"
 
-# Show recent chezmoi updates that have not been applied.
-LOGFILE=~/scripts/chezmoi/check-update.log
-TIMEINTERVAL=86400 # 1day
-
-if [ -f "$LOGFILE" ]; then
-  CURRENTTIME=$(date +%s)
-  FILETIME=$(gstat -c %Y $LOGFILE)
-  echo $FILETIME || "not exist" 
-  echo $CURRENTTIME || "not get current time"
-  TIMEDIFF=$(($CURRENTTIME - $FILETIME))
-
-  if [ $TIMEDIFF -lt $TIMEINTERVAL ]; then
-    echo "Recent changes in $LOGFILE:"
-    cat $LOGFILE
-  fi
-fi
 
 # Alias for Git
 alias gcb="git checkout -b"
@@ -47,7 +31,7 @@ alias currentBranch="git branch --contains"
 
 # Alias for zsh
 alias sz='source ~/.zshrc'
-alias vz='nvim ~/.zshrc'
+alias vz='vi ~/.zshrc'
 function mkdircd() {
   mkdir $1
   cd $1
